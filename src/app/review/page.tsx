import ReviewPage from "./page-client";

export const metadata = {
  title: "Review Queue | BrandArmor",
  description: "Internal human review queue for evidence-backed listing cases.",
};

export default function Page() {
  return <ReviewPage />;
}
