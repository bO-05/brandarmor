import DiscoveryPage from "./page-client";

export const metadata = {
  title: "Candidate Discovery | BrandArmor",
  description: "Find candidate marketplace leads without treating search as final proof.",
};

export default function Page() {
  return <DiscoveryPage />;
}
